s = 1
arr = ["hallo, klempner, das, ist, fantastisch, fluggegecheimen"]

st = input("Введите стоп слово = ")
arr[0] = arr[0].replace(" ","")
arr[0] = arr[0].replace(",","")

summa = len(arr[0])
for i in st:
    s *= arr[0].count(i)/summa

print(s)